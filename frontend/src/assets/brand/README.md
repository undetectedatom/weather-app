Place your custom header icon files in this folder.

Recommended options:
- `logo.svg`
- `logo.png`
- `logo@2x.png`

Current header behavior:
- if `frontend/src/assets/brand/logo.svg` exists and is wired later, it should replace the text badge
- until then, the app uses a simple fallback badge
